# UI Restructure Plan — GV Dispatch

> Scope: **Frontend UI/UX only**. ไม่แก้ TRCloud / Import / Sync / DB / Cron / Auth / Field mapping / Business logic / คำนวณเส้นทางใหม่

## Audit สรุป (โค้ดเดิม)

| พื้นที่ | สถานะเดิม | ช่องว่าง |
|--------|-----------|----------|
| ดึงออเดอร์ | API `syncTrcloudOrders` + bootstrap ใช้งานได้ | **ห้ามแตะ** |
| หน้างานส่ง | Card/ตาราง + selection bar มีแล้ว | ส่ง `dPick.sel` → `Plan.selected` แล้วยังให้เลือกซ้ำในหน้าจัดรถ |
| หน้าจัดรถ | Desktop 3 คอลัมน์ แต่ซ้ายเป็น “งานรอจัดทั้งหมด” + checkbox | ต้องเป็น **รายการที่เลือกแล้ว** ไม่บังคับเลือกใหม่ |
| Map | อยู่หน้าจัดรถ + ติดตาม | หน้างานส่งไม่มี map แล้ว — คงไว้เฉพาะจัดรถสำหรับวางแผน |
| Font | Prompt + IBM Plex Mono | เปลี่ยนเป็น **Tahoma, Arial, sans-serif** ตาม TRCloud |
| Touch | ปุ่ม ~40px, input ~42px | เป้า **≥48px** |

## Design Tokens (Phase 2)

```
font-family: Tahoma, Arial, sans-serif;
พื้นหลัง: #F5F7FA
Navigation: #0B1220
Primary: #B7E800
Success: เขียว + ข้อความสถานะ
กำหนดส่งวันนี้: ส้ม + ข้อความ
เลยกำหนด: แดง + ข้อความ

หัวข้อหน้า: 20px
ข้อมูลทั่วไป: 15–16px
ชื่อร้าน: 16px ตัวหนา
หัวตาราง: 12–13px
ข้อมูลตาราง: 13–14px
ปุ่ม / ช่องกรอก: สูงอย่างน้อย 48px
```

## Phase roadmap

| Phase | งาน | สถานะ |
|-------|------|--------|
| 1 | Audit + ไฟล์แผนนี้ | ✅ |
| 2 | Design Tokens (Tahoma, #F5F7FA, #B7E800, 48px) | ✅ |
| 3 | Desktop จัดรถ: ซ้ายเลือกแล้ว / กลาง Map / ขวารถ+คนขับ | ✅ |
| 4 | Mobile จัดรถ: wizard 5 ขั้น | ✅ |
| 5 | งานส่ง Card มือถือ | ✅ |
| 6 | ส่ง ID ที่เลือก → จัดรถ ไม่เลือกซ้ำ (`handoffSelectionToPlan`) | ✅ |
| 7 | ทดสอบกับข้อมูลจริง | รอผู้ใช้ |

## วิธีทดสอบ Phase 7 (ผู้ใช้)

1. เปิดหน้างานส่ง → เลือก 2+ บิล → กด **วางแผนรถ**
2. หน้าจัดรถต้องโชว์บิลเดิมทันที **ไม่มี checkbox เลือกใหม่**
3. Desktop: ซ้ายรายการ / กลางแผนที่ / ขวายืนยัน
4. Mobile: เลื่อนขั้น 1→5 ได้โดยไม่เลื่อนซ้ายขวา
5. ยืนยันว่าดึง TRCloud / ข้อมูลเดิมยังทำงาน


## Phase ถัดไป (ยังไม่ทำ)

- LINE Notification, รายงานใหม่, Print ใหม่, GPS, น้ำมัน, คำนวณเส้นทางใหม่, แก้ API/DB

## Acceptance Criteria

- [ ] ดึงออเดอร์เดิมได้เหมือนเดิม / ข้อมูลไม่หาย / ไม่แก้ API·DB
- [ ] Desktop อ่านง่าย · Mobile ไม่เลื่อนซ้ายขวา · ปุ่มกดง่าย (≥48px)
- [ ] เลือกออเดอร์แล้วไม่ต้องเลือกซ้ำหน้าจัดรถ
- [ ] Map เฉพาะหน้าจัดรถ (วางแผน) · ไม่โชว์ที่อยู่เต็มในหน้ารายการ
- [ ] Font Tahoma/Arial · สถานะมีข้อความ + สี · ข้อมูลจริง ไม่ใช้ Mock
